export default function ExampleProps(props) {
  return (
    <div>
      <p>Name: {props.name}</p>
      <p>Age: {props.age}</p>
      <p>Street: {props.street}</p>
      <p>Postal code: {props.postalCode}</p>
    </div>
  );
}
